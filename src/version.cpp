#include "silk/version.hpp"

namespace silk
{

// Definition provided so the library produces a translation unit to compile.
// Replace with real functionality as the project grows.
std::string_view library_version() noexcept
{
    return version;
}

}  // namespace silk
